public class FileInput{
/*

File i/o
The File Class
File f = new File("myFile.txt");

this class has some useful methods
exists() -return true

remember Scanner kb = new Scanner(System.in);
String in = kb.nextLine();
Scanner inFile = new Scanner(new File(in));

To actually get at whats int he file:
Scanner input = new Scanner(new File("input,txt"));
You must import both 
import java.utl.*;
/&/ import java.io.*;

public static void readInput90 throws FileNotFoundException 
PrintStream outFile = new PrintStream (new File(out));

Scanner inFile = new Scanner(new File(in));

while(inFile.hasNextLine()){
String line = inFile.nextLine();
System.out.println(line)

outFile
   System.out.println(inFile.nextLine);
   
   
   
   }
   
   
For Example output:

PrintStream outFile = new PrintStream (new File.